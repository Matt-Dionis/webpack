{
  "name": "vue-cli-template-webpack-apollo-graphql",
  "version": "1.0.0",
  "license": "MIT",
  "description": "A full-featured Webpack + vue-loader + Apollo GraphQL setup with hot reload, linting, testing & css extraction.",
  "scripts": {
    "docs": "cd docs && gitbook serve",
    "docs:deploy": "bash ./deploy-docs.sh"
  },
  "devDependencies": {
    "vue-cli": "^2.8.1"
  }
}
